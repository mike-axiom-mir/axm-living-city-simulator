# Assets

The v0.8 runtime uses deterministic CSS and canvas visuals rather than external image packs.

No online asset fetch is required. The game can later accept provenance-tracked local assets, but random internet-sourced assets are not part of this package.
